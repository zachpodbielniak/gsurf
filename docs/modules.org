#+TITLE: gsurf Modules
#+OPTIONS: toc:2 num:nil

Modules are loadable =.so= extensions — gsurf's equivalent of surf patches. Each
is enabled under the =modules:= section of the config. See [[file:module-system.org][module-system.org]] to
write one. =gsurf --list-modules= prints the registry in alphabetical order.

Status: *implemented* modules ship and are tested; *planned* modules are designed
(registry entry + config schema present) and follow the established pattern.

* Tier 1 — essential

| Module           | surf lineage         | Hook interface(s)                | Status      |
|------------------+----------------------+----------------------------------+-------------|
| =search_engines= | searchengines        | =GsurfUriHandler=                | implemented |
| =history=        | history              | =GsurfNavigationHook=            | implemented |
| =homepage=       | homepage             | =GsurfInputHandler=              | implemented |
| =toggles=        | toggle keybinds      | =GsurfInputHandler=              | implemented |
| =tabs=           | tabbed(1)            | =GsurfInputHandler=              | implemented |
| =chromebar=      | (address bar)        | =GsurfInputHandler= + window chrome | implemented |
| =adblock=        | (new; high value)    | =GsurfNavigationHook=            | implemented |
| =cookie_policy=  | togglecookiepolicy   | =GsurfInputHandler=             | implemented |
| =find_bar=       | WebKit find          | =GsurfInputHandler=             | implemented |

* Tier 2 — important

| Module        | surf lineage        | Status  |
|---------------+---------------------+---------|
| =modal=       | modal / vim keys + vimium link hints | implemented (default) |
| =adblock=     | (new; high value)   | implemented |
| =playexternal=| playexternal        | implemented |
| =omnibar=     | omnibar             | implemented |
| =bookmarks=   | bookmarking         | implemented |
| =externalpipe=| externalpipe        | implemented |
| =downloads=   | dlconsole/download  | implemented |
| =userscripts= | script.js           | implemented |
| =site_styles= | styles[]            | implemented |
| =uri_params=  | uriparams[]         | implemented |
| =cert_manager=| certs[]             | implemented |
| =useragent=   | useragent           | implemented |
| =notifications= | notifications     | implemented |
| =mcp=         | (new; AI control)   | implemented (=MCP=1=) |

* Tier 3 — nice-to-have

=spacesearch=, =inspector=, =dark_mode=, =status_bar=, =geolocation=,
=session=, and =proxy_switch= are implemented. =webgl_toggle= and
=dlconsole= are intentionally folded into =toggles= and =downloads=
respectively (they were redundant catalog entries).

All 29 modules build with =make= (the =mcp= module requires =MCP=1=).

* AI modules (deferred)

A future =AI=1= build flag (mirroring gst's =ai-glib= path) would add AI-powered
modules such as page-summary and chat. The architecture reserves room for it
(hook interfaces are already general enough); no AI modules are built yet.

* Per-module documentation

Detailed docs live under =docs/modules/<name>.org= as each module lands. The
=search_engines= and =mcp= modules are the reference implementations.

* Hosts-file parsing and download destinations

The adblock module accepts bare domains and hosts-file lines with an IPv4 or IPv6 address followed by one or more hostname aliases. It ignores blank lines and comments, including trailing =#= comments. Hostnames are normalized to lowercase ASCII (including internationalized domain names), with a trailing DNS root dot removed. Blocked and whitelisted domains match whole labels and their subdomains; whitelist matches take precedence. Invalid non-host tokens are excluded from generated filter JSON.

The downloads module treats the suggested name as a filename: it strips directory components, substitutes =download= for empty names, dot components and roots, and resolves relative =dir:= settings against the process working directory. Directory creation failures are logged and leave destination handling to the engine. This is filename containment, not a guarantee against another local process replacing directory entries; existing WebKit overwrite behavior is unchanged.

* Chrome module lifetime

The GTK3 =chromebar= and =status_bar= modules disconnect their window and view signals and destroy their widgets on deactivation. Reactivation creates one new widget. Disposal performs the same idempotent cleanup, including when an embedding host retains a window, view, or native widget after releasing its module manager. Modules retain their native widgets until cleanup so destroying the host container first does not leave dangling widget pointers.

Switching tabs refreshes the status bar from the new view's current load progress. Removing the last view clears both the address and status text.

The regression suite =tests/test-chrome-lifecycle.c= loads the real shared modules with model-only views and GTK containers, without launching a web process or making network requests. It is skipped by default; run =GSURF_TEST_GUI=1 ./build/release/test-chrome-lifecycle= with a display after =make test= to check tab state, deactivation, reactivation, retained widgets, and repeated disposal.
